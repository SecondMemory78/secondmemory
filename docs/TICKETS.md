# TICKETS.md — карта работ «крепкой базы»

Порядок по методологии: сначала швы и фундамент, потом фичи. Каждый тикет —
маленький вертикальный срез, закрывается через тест (red-green-refactor) + ручную прокликку.

## Фундамент (делаем строго по порядку)

- [x] **T1. Справочники врача → версионируемый JSON.**
  Извлечены `parameters.json` (279), `icd_urology.json` (553), `allergy_groups.json` (124).
  _Блокирует: T6, T8._

- [x] **T2. Шов сериализации.**
  Единый `dump()` вместо `.model_dump()` на «протухших» объектах (баг всплывал 3×).
  Регрессионный тест: create→dump возвращает все поля. _Блокирует: всё, что отдаёт JSON._

- [x] **T3. Шов времени (clock).**
  Убрать захардкоженное «сегодня»=2026-03-12. Ввести `clock.today()/now()`,
  автоопределение таймзоны с устройства (дефолт Europe/Moscow, хранить у врача).
  Seed генерирует демо-данные относительно настоящего «сегодня». _Блокирует: dashboard, assistant, calendar._

- [x] **T4 (бэкенд). Шов авторизации.** `current_doctor_id()` вместо DOCTOR_ID во всех
  роутерах (pure-ASGI middleware из Bearer-токена); модели Doctor(email,пароль,роль,tz),
  AuthSession, LoginCode; auth API (register/login/verify/me/logout); хеш пароля PBKDF2.
  Тесты: изоляция врачей по токену, вход по коду, известное устройство без кода.
- [x] **T4b (фронт). Экран входа.** email+пароль+код, «запомнить устройство», токен в Authorization, быстрый разблок PIN/биометрия (WebAuthn), выбор специальности при регистрации, реальный logout.
  `current_doctor` вместо `DOCTOR_ID=1`. Модель Doctor + specialty (выбор при регистрации).
  Роли doctor/support/admin. Реальный вход: email+пароль+код на почту, «запомнить устройство»
  (токен 90 дней), локальный разблок биометрия/PIN (WebAuthn/passkey; нативно в Capacitor).
  _Блокирует: мультиврач, 152-ФЗ по-настоящему._

## Доменная модель

- [x] **T5. Визит (Encounter).** Сущность визита; привязка наблюдений/заметок/назначений/
  протокола/документов; вкладка «История визитов». _Зависит: T2._

- [x] **T6. Множественные диагнозы.** PatientDiagnosis (МКБ+версия+формулировка+статус+дата),
  «основной» в шапке, история без затирания; выбор из `icd_urology.json`. _Зависит: T1, T2, T5._

- [x] **T7. Параметры из справочника.** Заменить захардкоженные метки на `parameters.json`
  (отображение, синонимы для OCR, пороги). _Зависит: T1._

- [x] **T8. Аллергобезопасность по справочнику.** Группы/компоненты/перекрёстность/скрытые
  аллергены; предупреждение + причина в аудит; без автозамен. _Зависит: T1._

## Фичи (после фундамента)
- [ ] Графики в админ-панели (по дням, фильтры).
- [ ] Мультимодальный захват (фото направления → событие/задача).
- [ ] Эскалация напоминаний: push → push → звонок через **Telegram и MAX-бота**.
- [ ] Обёртка Capacitor → RuStore/Google Play, затем App Store.

## Легенда
Каждый тикет закрывается только когда: зелёный тест + ручная прокликка + «работает гладко».
